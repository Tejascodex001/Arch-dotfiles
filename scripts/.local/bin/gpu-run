#!/bin/bash
# Run any command on NVIDIA GPU without switching modes or rebooting
# Usage: gpu-run python script.py
#        gpu-run ollama run llama3
__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia "$@"
